

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Yuuuuuuo")
            Text("jodsdncsdc")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
